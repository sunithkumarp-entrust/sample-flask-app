name: 'Fetch Secret'
description: 'Fetch a secret from an external vault'
inputs:
  base-url:
    description: 'The base URL of the vault API'
    required: true
  api-token:
    description: 'The API token for authentication'
    required: true
  box-id:
    description: 'The box ID to fetch the secret from'
    required: true
  secret-id:
    description: 'The secret ID to fetch'
    required: true
  ca-cert:
    description: 'The CA certificate for SSL verification'
    required: false
outputs:
  secret:
    description: 'The fetched secret'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  color: 'blue'
  icon: 'lock'
